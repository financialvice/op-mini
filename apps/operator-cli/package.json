{
  "name": "@camglynn/operator-cli",
  "version": "0.0.1",
  "type": "module",
  "description": "Operator CLI",
  "license": "MIT",
  "bin": {
    "operator": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "typecheck": "tsgo --noEmit",
    "build": "tsup",
    "test:e2e": "bun run build && bun run scripts/test-e2e.ts",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@trigger.dev/sdk": "catalog:",
    "commander": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsup": "catalog:"
  }
}
